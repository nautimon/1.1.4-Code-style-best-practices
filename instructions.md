Each line of code here has one (and only one) formatting or syntax error in it. 

Look closely at the whitespace, punctuation, brackets/parentheses/etc and see if you can catch the fix required to make each line follow formatting best practices. 

Remember, JavaScript doesn't care about whitespace, so your code might run just fine, but still have a style error. Make sure to have an instructor or teaching assistant check your solution at the end!